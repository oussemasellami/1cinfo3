{% extends 'base.html.twig' %}

{% block title %}Classroom{% endblock %}

{% block body %}
    <h1>Classroom</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ classroom.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ classroom.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_classroom_index') }}">back to list</a>

    <a href="{{ path('app_classroom_edit', {'id': classroom.id}) }}">edit</a>
    <a href="{{ path('app_showclassroomstudent', {'id': classroom.id}) }}">edit</a>
    {{ include('classroom/_delete_form.html.twig') }}
{% endblock %}
